
export const User = {
    id: number,
    email: String,
    Name: String,
}
export const AppData = {
    authToken: String,
    isAuthenticated: boolean,
    isAdmin: boolean,
    isLoading: boolean,
    loginMessage: String,
}